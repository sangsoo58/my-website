# V3.3.1 배포

1. 이 ZIP의 내용 전체를 기존 `my-website` 루트에 복사합니다.
2. 동일 파일은 `대상 폴더의 파일 바꾸기`를 선택합니다.
3. GitHub Desktop에서 변경 파일을 확인합니다.
4. Commit: `V3.3.1 fix CASE Library two-axis filters`
5. Push origin 후 GitHub Pages 배포를 기다립니다.
6. 사이트에서 Ctrl+Shift+R 후 `절감 사례` 섹션을 확인합니다.

CNAME/DNS/GitHub Pages 설정은 변경하지 마십시오.
